# 功能优化 — 待实现想法

## 1. 多模式着色
点云查看器切换着色模式：RGB原色 / 深度伪彩色 / 置信度热力图 / 法向方向色

## 2. ~~模型绕Y轴自动旋转~~ ✅ 已完成
3D查看器增加"转台动画"按钮，模型绕Y轴自动旋转。底部HUD栏 `↻ 旋转` 按钮，点击切换，`autoRotateSpeed=0.6`

## 3. ~~深度图预览~~ ✅ 已完成
融合前在Web端预览各视角深度图（伪彩色），采样5张Turbo伪彩色PNG，支持灯箱放大和键盘导航。COLMAP路径读`.geometric.bin`，MVS路径直接用numpy数组。



## 5. 交互式置信度过滤
前端滑块实时调整置信度阈值，动态增减显示的点云



## 7. 重建质量报告
自动生成HTML/PDF报告：输入缩略图、点云数量统计、置信度分布直方图

## 8. 图像质量预检
上传后检测模糊度（Laplacian方差）、估算重叠率，给出补充拍摄建议

## 9. ~~视频输入~~ ✅ 已完成
手机绕物体拍一圈视频，自动切片+智能帧筛选，送入重建管道。`hybridmvs/video_processor/` 模块已实现（extractor → selector → preprocessor），前端 UploadPanel 支持视频 Tab，后端 tasks.py 已集成
